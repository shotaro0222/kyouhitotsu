{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "normalize:data": "node ./scripts/normalize-data-json.mjs",
    "predev": "npm run normalize:data",
    "predeploy": "npm run normalize:data",
    "pretest": "npm run normalize:data",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.110.0"
  }
}